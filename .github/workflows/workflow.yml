name: Deploy to Production
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        timeout-minutes: 1
        run: |
           echo "Deploying to production..." 
           sleep 600s
